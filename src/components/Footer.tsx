const Footer = () => {
  return (
    <footer className="py-10 text-center opacity-30 text-[10px] tracking-[0.3em] uppercase w-full">
      © {new Date().getFullYear()} LUKA BRADIC // ALL RIGHTS RESERVED
    </footer>
  );
};

export default Footer;
